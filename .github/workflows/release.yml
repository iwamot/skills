name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@6dd705693b7a451242519165c90446d73a6870c8 # v8.10.0
    permissions:
      contents: write  # to create the GitHub Release
