name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@55c690f55783f59fcb2f2309d8c9feebb13bdc7d # v8.3.0
    permissions:
      contents: write  # to create the GitHub Release
