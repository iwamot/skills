name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@4fc6b72645ab28986db27f0d96fc94634fda3853 # v8.8.1
    permissions:
      contents: write  # to create the GitHub Release
