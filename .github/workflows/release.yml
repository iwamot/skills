name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@ae85e64823f8d8316a33fa549c7a5ec13ab56795 # v8.6.1
    permissions:
      contents: write  # to create the GitHub Release
