name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@10f59b7bfb593a0a124fd07fad3dee3d38599ceb # v8.8.3
    permissions:
      contents: write  # to create the GitHub Release
