name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@7e1e1b73633f829ea0ac1ca934aa4d085561d61a # v7.1.0
    permissions:
      contents: write  # to create the GitHub Release
