name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@cbf06d35d04e4ad85d157a5cda1484876d1a71f2 # v8.8.2
    permissions:
      contents: write  # to create the GitHub Release
