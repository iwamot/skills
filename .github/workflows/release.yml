name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@e1011cc73c85c693d3f26dc243c7cc26e66aa67b # v8.5.0
    permissions:
      contents: write  # to create the GitHub Release
