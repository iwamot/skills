name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@3a60350ff82e769fc95271d672a481d135d0c059 # v8.8.4
    permissions:
      contents: write  # to create the GitHub Release
