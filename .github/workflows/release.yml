name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@e7256362ed2fb132963d7c3c977dfe2644f09c58 # v8.0.3
    permissions:
      contents: write  # to create the GitHub Release
