name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@fe9df89e74648e67a0cda3f708718e9f11db2ce7 # v8.0.2
    permissions:
      contents: write  # to create the GitHub Release
