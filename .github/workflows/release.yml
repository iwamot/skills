name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@03b94c328c99fb628465841debd7ba83ca9b517f # v8.10.1
    permissions:
      contents: write  # to create the GitHub Release
