name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@dc2b7bd58882cf331fdf66a716ca7063eaf975da # v8.5.1
    permissions:
      contents: write  # to create the GitHub Release
