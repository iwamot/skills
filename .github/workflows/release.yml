name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@f4bb8660a94f68c9ac2228d5fad6b990adc922ff # v8.6.0
    permissions:
      contents: write  # to create the GitHub Release
