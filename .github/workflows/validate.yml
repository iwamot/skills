name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/validate.yml@55c690f55783f59fcb2f2309d8c9feebb13bdc7d # v8.3.0
