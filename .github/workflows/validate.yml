name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/validate.yml@cbf06d35d04e4ad85d157a5cda1484876d1a71f2 # v8.8.2
